apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: artist-worker
  labels:
    app: artist-worker
spec:
  rules:
  - http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: artist-worker
              port:
                number: 3000